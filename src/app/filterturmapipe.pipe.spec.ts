import { FilterturmapipePipe } from './filterturmapipe.pipe';

describe('FilterturmapipePipe', () => {
  it('create an instance', () => {
    const pipe = new FilterturmapipePipe();
    expect(pipe).toBeTruthy();
  });
});
